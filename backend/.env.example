# MongoDB Configuration
MONGO_URI=mongodb+srv://purnimasaipinnamaraju_db:NDk6XguKN4qkoMXE@cluster0.8g0z4hp.mongodb.net/?appName=Cluster0

# JWT Secret - MUST BE CHANGED FOR PRODUCTION
# Generate using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=change_this_to_a_long_random_secret_minimum_32_characters

# Server Configuration
PORT=5050
NODE_ENV=development

# Frontend URL (update for production deployment)
FRONTEND_URL=http://localhost:3000

# Logging Configuration
LOG_LEVEL=info

# Email Configuration (Optional - for notifications)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
